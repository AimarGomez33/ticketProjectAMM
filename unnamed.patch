Index: app/src/main/java/com/example/ticketapp/MainActivity.kt
IDEA additional info:
Subsystem: com.intellij.openapi.diff.impl.patch.CharsetEP
<+>UTF-8
===================================================================
diff --git a/app/src/main/java/com/example/ticketapp/MainActivity.kt b/app/src/main/java/com/example/ticketapp/MainActivity.kt
--- a/app/src/main/java/com/example/ticketapp/MainActivity.kt	
+++ b/app/src/main/java/com/example/ticketapp/MainActivity.kt	(date 1769991237294)
@@ -8,7 +8,10 @@
 import android.bluetooth.BluetoothDevice
 import android.bluetooth.BluetoothManager
 import android.bluetooth.BluetoothSocket
-import android.content.*
+import android.content.BroadcastReceiver
+import android.content.Context
+import android.content.Intent
+import android.content.IntentFilter
 import android.content.pm.PackageManager
 import android.hardware.usb.UsbConstants
 import android.hardware.usb.UsbDevice
@@ -26,18 +29,23 @@
 import android.view.ViewGroup
 import android.view.ViewStub
 import android.view.animation.AccelerateDecelerateInterpolator
-import android.widget.*
 import android.widget.AdapterView
 import android.widget.ArrayAdapter
 import android.widget.Button
 import android.widget.CheckBox
 import android.widget.EditText
+import android.widget.FrameLayout
 import android.widget.GridLayout
+import android.widget.ImageButton
+import android.widget.ImageView
 import android.widget.LinearLayout
+import android.widget.Spinner
 import android.widget.TextView
+import android.widget.Toast
 import androidx.activity.result.contract.ActivityResultContracts
 import androidx.appcompat.app.AlertDialog
 import androidx.appcompat.app.AppCompatActivity
+import androidx.compose.ui.semantics.text
 import androidx.core.content.ContextCompat
 import androidx.drawerlayout.widget.DrawerLayout
 import androidx.lifecycle.lifecycleScope
@@ -46,18 +54,18 @@
 import com.google.android.material.bottomsheet.BottomSheetDialog
 import com.google.android.material.button.MaterialButton
 import com.google.android.material.card.MaterialCardView
+import kotlinx.coroutines.CoroutineScope
+import kotlinx.coroutines.Dispatchers
+import kotlinx.coroutines.launch
+import kotlinx.coroutines.withContext
 import java.io.IOException
 import java.io.OutputStream
 import java.nio.charset.Charset
 import java.text.SimpleDateFormat
-import java.util.*
 import java.util.Calendar
 import java.util.Date
 import java.util.Locale
-import kotlinx.coroutines.CoroutineScope
-import kotlinx.coroutines.Dispatchers
-import kotlinx.coroutines.launch
-import kotlinx.coroutines.withContext
+import java.util.UUID
 
 
 class MainActivity : AppCompatActivity() {
@@ -242,6 +250,7 @@
 
     private lateinit var editCategoryStartDate: EditText
     private lateinit var editCategoryEndDate: EditText
+    lateinit var TxtTotal: TextView
 
     private fun recalcHamburguesasTotalYResumen() {
         var total = 0.0
@@ -257,9 +266,23 @@
         mostrarResumen(productosSeleccionados)
     }
 
+    private fun getRandomColor(): Int {
+        val colors = listOf(
+            R.color.Grayish_lime,
+            R.color.Light_grayish,
+            R.color.old_grayish_green,
+            R.color.Grayish_green,
+            R.color.Grey
+        )
+        return ContextCompat.getColor(this, colors.random())
+    }
+
+
+
     override fun onCreate(savedInstanceState: Bundle?) {
         super.onCreate(savedInstanceState)
         setContentView(R.layout.activity_main)
+        TxtTotal = findViewById(R.id.textViewTotal)
 
         setupCategoryProfitPanel()
         // setupAdminGananciaPorCategoria() // Replaced by Top Selling Panel setup later
@@ -527,76 +550,73 @@
 
     /** Muestra un diálogo para ingresar la cantidad manualmente (Teclado numérico). */
     private fun showQuantityDialog(productName: String, currentQty: Int, onConfirm: (Int) -> Unit) {
-        val input = EditText(this)
-        input.inputType = android.text.InputType.TYPE_CLASS_NUMBER
-        input.setText(if (currentQty > 0) currentQty.toString() else "")
-        input.selectAll()
-        input.gravity = Gravity.CENTER
-        input.textSize = 24f
+            val input = EditText(this)
+            input.inputType = android.text.InputType.TYPE_CLASS_NUMBER // Fuerza el Numeric Pad
+            input.setText(if (currentQty > 0) currentQty.toString() else "")
+            input.setSelection(input.text.length)
+            input.gravity = Gravity.CENTER
+            input.textSize = 24f
 
-        // Contenedor para margenes
-        val container = FrameLayout(this)
-        val params =
-                FrameLayout.LayoutParams(
-                        ViewGroup.LayoutParams.MATCH_PARENT,
-                        ViewGroup.LayoutParams.WRAP_CONTENT
-                )
-        params.leftMargin = 50
-        params.rightMargin = 50
-        input.layoutParams = params
-        container.addView(input)
+            val container = FrameLayout(this)
+            val params = FrameLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.WRAP_CONTENT)
+            params.setMargins(50, 20, 50, 10)
+            input.layoutParams = params
+            container.addView(input)
 
-        val dialog =
-                AlertDialog.Builder(this)
-                        .setTitle(productName)
-                        .setMessage("Ingresa la cantidad:")
-                        .setView(container)
-                        .setPositiveButton("Aceptar") { _, _ ->
-                            val text = input.text.toString()
-                            val qty = text.toIntOrNull() ?: 0
-                            onConfirm(qty)
-                        }
-                        .setNegativeButton("Cancelar", null)
-                        .create()
-
-        // Mostrar el teclado automáticamente
-        dialog.window?.setSoftInputMode(
-                android.view.WindowManager.LayoutParams.SOFT_INPUT_STATE_VISIBLE
-        )
-        dialog.show()
-    }
+            AlertDialog.Builder(this)
+                .setTitle(productName)
+                .setMessage("Ingresa la cantidad:")
+                .setView(container)
+                .setPositiveButton("Aceptar") { _, _ ->
+                    val newValue = input.text.toString().toIntOrNull() ?: 0
+                    onConfirm(newValue)
+                }
+                .setNegativeButton("Cancelar", null)
+                .show()
+        }
 
+    /** Muestra un diálogo para ingresar la cantidad manualmente (Teclado numérico). */
+
+    /** Cambia el color de fondo de la tarjeta si hay productos seleccionados. */
     /** Cambia el color de fondo de la tarjeta si hay productos seleccionados. */
     private fun updateCardAppearance(view: View?, quantity: Int) {
         if (view == null) return
         try {
-            // Estructura esperada: TextView -> LinearLayout (horizontal) -> LinearLayout (vertical)
-            // -> MaterialCardView
-            // Ajustar según tu XML (activity_main.xml)
-            // Layout paso a paso:
-            // 1. TextView (cantidad) parent -> LinearLayout (botones + texto)
-            // 2. Parent -> LinearLayout (label + controles)
-            // 3. Parent -> MaterialCardView
-            val parent1 = view.parent as? ViewGroup // LinearLayout horizontal
-            val parent2 = parent1?.parent as? ViewGroup // LinearLayout vertical
-            val card = parent2?.parent as? MaterialCardView // La Card
+            // Buscamos la Card subiendo en la jerarquía del XML
+            val parent1 = view.parent as? ViewGroup
+            val parent2 = parent1?.parent as? ViewGroup
+            val card = parent2?.parent as? MaterialCardView
 
             if (card != null) {
                 if (quantity > 0) {
-                    // Color resaltado (ej: Verdoso suave o Amber suave)
-                    card.setCardBackgroundColor(ContextCompat.getColor(this, R.color.Grayish_lime))
-                    // O usar un color definido: ContextCompat.getColor(this, R.color.tu_color)
+                    // Solo asignamos un color nuevo si la card tiene el color de fondo por defecto
+                    // Esto evita que el color cambie ("parpadee") cada vez que pulsas "+"
+                    val defaultColor = ContextCompat.getColor(this, R.color.card_background) // o tu color base
+
+                    // Si quieres que SIEMPRE sea aleatorio al primer click:
+                    val palette = listOf(
+                        R.color.palette_sage,
+                        R.color.palette_cream,
+                        R.color.palette_peach,
+                        R.color.palette_mint,
+                        R.color.palette_sand,
+                        R.color.palette_lemon
+                    )
+
+                    // Solo cambiamos el color si es la primera vez que se selecciona (estaba en 0)
+                    // O si quieres que cambie siempre, simplemente quita el 'if'
+                    card.setCardBackgroundColor(ContextCompat.getColor(this, palette.random()))
+
                 } else {
-                    // Color default
-                    card.setCardBackgroundColor(
-                            ContextCompat.getColor(this, R.color.card_background)
-                    )
+                    // Volver al color neutro cuando la cantidad es 0
+                    card.setCardBackgroundColor(ContextCompat.getColor(this,  R.color.card_background))
                 }
             }
         } catch (e: Exception) {
-            // Ignorar errores de casting si la jerarquía cambia
+            Log.e("ColorError", "Error al cambiar color: ${e.message}")
         }
     }
+
 
     private fun setupHamburguesasGrid(grid: GridLayout) {
         grid.removeAllViews()
@@ -3084,13 +3104,6 @@
                                 root.findViewById(R.id.btnMasAlitas15),
                                 140.0
                         )
-                products["Combo"] =
-                        ProductData(
-                                root.findViewById(R.id.cantidadCombo),
-                                root.findViewById(R.id.btnMenosCombo),
-                                root.findViewById(R.id.btnMasCombo),
-                                30.0
-                        )
             }
             "Bebidas" -> {
                 products["Refrescos"] =
@@ -3271,10 +3284,28 @@
             val tvCantidad = data.cantidadTV as? TextView
 
             fun updateProductState() {
-                val qty = quantities[nombre] ?: 0
-                tvCantidad?.text = qty.toString()
-                updateCardAppearance(tvCantidad, qty)
-                recalculateAndUpdateAll()
+
+
+                products.forEach { (nombre, data) ->
+
+                    data.TextView.setOnClickListener {
+                        val qtyActual = quantities[nombre] ?: 0
+
+                        showQuantityDialog(nombre, qtyActual) { nuevaCantidad ->
+                            // 1. Actualizar el mapa de cantidades
+                            quantities[nombre] = nuevaCantidad
+
+                            // 2. Actualizar el texto visual
+                            data.txtTotal.text = nuevaCantidad.toString()
+
+                            // 3. Aplicar color aleatorio o default si es > 0 (tu función anterior)
+                            updateCardAppearance(data.textView, nuevaCantidad)
+
+                            // 4. Recalcular el total de la app
+                            recalculateAndUpdateAll()
+                        }
+                    }
+                }
             }
 
             data.btnMas.setOnClickListener {
